import './index.scss';

const BreatheTitle = () => {
	return <section className='breathe-title'>Press the keyboard ↑ or touch the screen to start the experience</section>;
};

export default BreatheTitle;
